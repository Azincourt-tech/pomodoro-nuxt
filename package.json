{
  "name": "pomodoro-nuxt",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "cd apps/web && npx vitest run",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.9.3"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.1"
  }
}
