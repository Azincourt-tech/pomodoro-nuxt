{
  "name": "pomodoro-nuxt",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "cd apps/web && npx vitest run",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.9.3"
  },
  "dependencies": {
    "@nuxt/icon": "^2.2.1",
    "@opentelemetry/api": "^1.9.1"
  }
}
