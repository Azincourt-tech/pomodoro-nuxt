{
  "name": "@pomodoro/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate": "wrangler d1 migrations apply pomodoro-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply pomodoro-db --remote"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.2",
    "wrangler": "^3.90.0"
  }
}
