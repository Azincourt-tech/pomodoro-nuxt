{
  "builds": [
    {
      "src": "apps/web/**",
      "use": "@vercel/node",
      "config": { "distDir": "apps/web/.output" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/apps/web/$1" }
  ]
}
